# Copy to .env and fill in. .env is gitignored — never commit real secrets.

# LLM / AI workflows
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# AWS (uk_address_matcher pulls some datasets from S3 via boto3)
AWS_PROFILE=
AWS_REGION=eu-west-2
